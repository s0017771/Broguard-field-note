# 📇 명함 → 연락처 (card.html)

폰으로 명함을 찍으면 Claude Vision이 정보를 읽어 vCard(.vcf)로 만들어 주고,
버튼 한 번으로 폰 연락처에 저장할 수 있는 웹앱입니다.

- 카카오톡 · 구글 드라이브 연동 **불필요** (프로그램에서 바로 다운로드)
- 서버로 나가는 건 OCR 1건뿐 — 그 외엔 전부 폰 안에서 처리
- 이 저장소의 기존 "현장조사노트" 앱과 별도로 동작 (`card.html`)

## 구조

```
[폰 브라우저] card.html
  1. 카메라로 명함 촬영
  2. 브라우저에서 작게 리사이즈 (약 1400px, JPEG)
  3. → /api/ocr (Claude Vision) → 명함정보 JSON
  4. 화면에서 오타 확인/수정
  5. "연락처로 저장" → .vcf 다운로드 → 폰 연락처에 추가
```

| 파일 | 역할 |
|------|------|
| `card.html` | 폰 UI (촬영·수정·다운로드) — 정적 파일 |
| `api/ocr.js` | Vercel 서버리스 함수, Claude Vision 호출 |
| `vercel.json` | 함수 타임아웃(30초) 설정 |

## 배포 (최초 1회)

1. **Vercel에 이 저장소 연결** (이미 Vercel 쓰고 계시면 New Project → 이 repo 선택)
2. **환경변수 추가** — Vercel 프로젝트 → Settings → Environment Variables
   - Name: `ANTHROPIC_API_KEY`
   - Value: `sk-ant-...` (console.anthropic.com 에서 발급)
   - 저장 후 **Redeploy**
3. 배포되면 폰에서 접속:
   - `https://<프로젝트>.vercel.app/card.html`
4. (선택) 폰 브라우저에서 **홈 화면에 추가** → 앱 아이콘처럼 사용

## 사용법

1. 홈 화면 아이콘(또는 링크) 열기
2. **📷 명함 촬영 / 선택** → 명함 찍기
3. 몇 초 뒤 추출된 정보가 뜸 → 틀린 곳 있으면 수정
4. **💾 연락처로 저장** → `.vcf` 다운로드 → 파일 열기 → 연락처에 추가

## 참고 / 주의

- **API 키는 서버(Vercel 환경변수)에만** 있습니다. 폰(HTML)에는 노출되지 않아요.
- 사진은 업로드 전에 브라우저에서 작게 리사이즈합니다 → Vercel 함수 본문 4.5MB
  제한을 넘지 않고, OCR도 빨라집니다.
- 모델은 `claude-opus-4-8` 을 사용합니다. 비용을 더 아끼고 싶으면 `api/ocr.js`
  의 `model` 을 `claude-haiku-4-5` 로 바꾸면 됩니다 (정확도/비용 트레이드오프).
- 정확도 규칙(추측 금지, 없는 정보 생성 금지, 전화번호 하이픈 표준)은
  `api/ocr.js` 의 시스템 프롬프트에 반영돼 있습니다.
- iOS/Android 최신 브라우저에서 `.vcf` 다운로드 → 열기 → 연락처 추가가 됩니다.
