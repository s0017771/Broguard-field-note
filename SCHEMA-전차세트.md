# 전차세트 JSON 스키마 (정밀점검 재촬영 · RE-INSPECT PRO)

앱(reinspect-pro.html)과 생성 스크립트(차장님 방: autolink_fix.py / autolink_ocr.py) 사이의 **계약 문서**.
형식을 바꿀 때는 반드시 양쪽 합의 후 이 문서를 갱신한다. (현재 버전: v3)

## 최상위
```json
{
  "교량": "강매교",
  "기준회차": "정밀안전점검(전차)",
  "v": 3,
  "도면정보": { "<pageIndex>": { "번호": 96, "이름": "교각 P5" } },
  "slots": [ ... ]
}
```
- `도면정보` (선택): 망도 페이지 이름 지정. **키 = 망도 PDF의 0기준 페이지 인덱스**
  (pin.pageIndex 와 동일 규약). `번호`는 문자열("096")·정수(96) 모두 허용(앱이 정수 표시).
  표지·간지는 `번호: null` + 이름 앞 `"§ "` 접두. OCR 신뢰도가 낮으면 항목 자체를
  생략 → 앱이 자체 추출(텍스트 표제란)·간지 텍스트·파일명 순으로 폴백.

## slots[] — 전경
```json
{ "grp": "전경", "label": "교량받침 P2 전경", "note": "", "ref": "data:image/jpeg;base64,..." }
```

## slots[] — 외관조사(손상)
```json
{
  "grp": "외관조사",
  "num": "도면96-5",
  "위치": "P5", "부재": "교각", "도면": "96", "연번": "5",
  "label": "P5 교각 균열(0.3mm미만) 신규0.2㎜, 1.3m, 4EA",
  "prev비고": "",
  "물량": { "폭": "0.2", "너비": "", "길이": "1.3", "개소": "4" },
  "pin": { "pageIndex": 37, "x": 0.481, "y": 0.419 },
  "ref": "data:image/jpeg;base64,..."
}
```
- `pin` (선택): 자동 배치 좌표. `pageIndex` = 망도 PDF 0기준 페이지, `x`/`y` = 페이지
  표시 방향 기준 0~1 정규화 좌표(회전 보정 후). 없으면 앱 ⚡빠른배치 대상.
- `물량` (선택): 없으면 앱이 label에서 자동 파싱(cw=·L=·N×M·EA 패턴).
- `ref`: 전차 사진 dataURI(JPEG). 긴 변 560px 내외 권장(세트 용량 관리).

## 앱 동작 규약
- 페이지 이름 우선순위: **도면정보 > 앱 표제란 추출 > 간지 텍스트(§) > 파일명**
- 핀 연결: 세트·망도 어느 순서로 불러와도 pageIndex → 실제 페이지로 해석(reconcile)
- 앱이 내보내는 야장 JSON은 위 필드에 판정/메모/shot/added 를 더해 반환(사진 제외)

## 이력
- v3 (2026-08): pin 자동 배치 + 도면정보 필드 도입 (북로IC C 100%, B1 87% 자동 배치)
- v2 (2026-08): 사진첩 OCR 파싱(위치·부재·도면·연번 필드)
- v1 (2026-08): 정기점검 재촬영용 기본形 (grp/num/label/ref)
